# 技术播客音频配置指南

## 问题说明

当前播客使用的是占位符音频URL（`https://example.com/...`），无法实际播放。需要替换为真实的音频文件。

## 解决方案

### 方案 1: 使用公开的英语学习音频（推荐）

#### BBC Learning English
BBC 提供免费的英语学习音频资源：

```sql
-- 在 Supabase SQL Editor 中执行
UPDATE tech_podcasts
SET audio_url = 'https://downloads.bbc.co.uk/learningenglish/features/6min/230831_6min_english_ai_and_music_download.mp3'
WHERE title = 'The Future of AI: Opportunities and Challenges';
```

#### 其他公开资源
- **VOA Learning English**: https://learningenglish.voanews.com/
- **TED Audio**: https://www.ted.com/talks (需要下载后上传)
- **Podcast 公开Feed**: 很多播客提供公开的音频链接

### 方案 2: 上传自己的音频文件

#### 步骤 1: 准备音频文件
1. 格式：MP3（推荐）、WAV、OGG
2. 时长：5-10 分钟
3. 大小：建议 < 10MB
4. 质量：建议 128kbps 以上

#### 步骤 2: 上传到云存储

**选项 A: 使用 Supabase Storage**

```bash
# 1. 在 Supabase Dashboard 创建 Storage Bucket
# 2. 上传音频文件
# 3. 设置为公开访问
# 4. 复制公开URL
```

在 Supabase Dashboard:
1. 进入 Storage
2. 创建新 Bucket: `podcast-audio`
3. 设置为 Public
4. 上传音频文件
5. 获取公开URL（类似）：
   ```
   https://your-project.supabase.co/storage/v1/object/public/podcast-audio/ai-future.mp3
   ```

**选项 B: 使用其他云存储**
- AWS S3
- Google Cloud Storage
- Cloudflare R2
- 阿里云 OSS

#### 步骤 3: 更新数据库

```sql
UPDATE tech_podcasts
SET audio_url = 'YOUR_AUDIO_URL_HERE'
WHERE title = 'The Future of AI: Opportunities and Challenges';
```

### 方案 3: 使用文字转语音服务

如果没有音频，可以使用 TTS 服务将文本转为语音：

#### OpenAI TTS
```javascript
// 使用 OpenAI API 生成语音
const response = await fetch('https://api.openai.com/v1/audio/speech', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    model: 'tts-1',
    voice: 'alloy',
    input: 'Your podcast transcript text...'
  })
});
```

#### Google Cloud TTS
- 访问: https://cloud.google.com/text-to-speech
- 支持多种语言和声音
- 免费额度：每月 400 万字符

#### ElevenLabs
- 访问: https://elevenlabs.io/
- 高质量 AI 语音
- 免费试用

### 方案 4: 下载 TED 演讲音频

#### 步骤：
1. 访问 TED 官网: https://www.ted.com/talks
2. 找到相关演讲
3. 使用浏览器开发者工具查找音频URL
4. 或使用下载工具如 youtube-dl

```bash
# 示例：使用 yt-dlp 下载
yt-dlp -f 'bestaudio[ext=m4a]' 'TED_TALK_URL'
```

## 快速测试

### 临时测试音频
使用这些公开的测试音频快速验证功能：

```sql
-- 测试音频 1: 短音频示例
UPDATE tech_podcasts
SET audio_url = 'https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3'
WHERE id = 'YOUR_PODCAST_ID';

-- 测试音频 2: 语音示例
UPDATE tech_podcasts
SET audio_url = 'https://www2.cs.uic.edu/~i101/SoundFiles/BabyElephantWalk60.wav'
WHERE id = 'YOUR_PODCAST_ID';
```

**注意：** 这些是测试音频，不是真实的播客内容，仅用于验证播放器功能。

## 推荐工作流程

### 方案 A: 完全免费（使用公开资源）
```
1. 找到公开的英语学习音频
   ↓
2. 复制音频URL
   ↓
3. 更新数据库
   ↓
4. 测试播放
```

### 方案 B: 自制内容（推荐）
```
1. 编写播客文本（或使用现有文本）
   ↓
2. 使用 TTS 服务生成音频
   ↓
3. 上传到 Supabase Storage
   ↓
4. 更新数据库
   ↓
5. 添加生词和听写片段
```

## 完整示例：添加新播客

### 1. 生成音频（使用 OpenAI TTS）

```javascript
// Node.js 脚本
import OpenAI from 'openai';
import fs from 'fs';

const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY
});

async function generatePodcastAudio() {
  const text = `Good morning everyone. Today I want to talk about...`; // 完整文本

  const mp3 = await openai.audio.speech.create({
    model: "tts-1",
    voice: "alloy",
    input: text,
  });

  const buffer = Buffer.from(await mp3.arrayBuffer());
  await fs.promises.writeFile('podcast.mp3', buffer);
  console.log('Audio generated: podcast.mp3');
}

generatePodcastAudio();
```

### 2. 上传到 Supabase

```javascript
// 上传脚本
import { createClient } from '@supabase/supabase-js';
import fs from 'fs';

const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL,
  process.env.SUPABASE_SERVICE_KEY
);

async function uploadAudio() {
  const file = fs.readFileSync('podcast.mp3');

  const { data, error } = await supabase.storage
    .from('podcast-audio')
    .upload('ai-future.mp3', file, {
      contentType: 'audio/mpeg',
      cacheControl: '3600',
    });

  if (error) {
    console.error('Upload error:', error);
  } else {
    console.log('Uploaded:', data);
    const url = `${process.env.NEXT_PUBLIC_SUPABASE_URL}/storage/v1/object/public/podcast-audio/ai-future.mp3`;
    console.log('Public URL:', url);
  }
}

uploadAudio();
```

### 3. 更新数据库

```sql
UPDATE tech_podcasts
SET audio_url = 'https://your-project.supabase.co/storage/v1/object/public/podcast-audio/ai-future.mp3'
WHERE title = 'The Future of AI: Opportunities and Challenges';
```

## 检查音频是否可用

### 方法 1: 浏览器测试
直接在浏览器地址栏输入音频URL，看是否能播放

### 方法 2: 使用 curl
```bash
curl -I 'YOUR_AUDIO_URL'
# 检查返回的 Content-Type 是否为 audio/...
```

### 方法 3: 在控制台测试
```javascript
// 浏览器控制台
const audio = new Audio('YOUR_AUDIO_URL');
audio.play().then(() => {
  console.log('Audio is playable!');
}).catch(err => {
  console.error('Audio error:', err);
});
```

## 常见问题

### Q: 音频URL正确但还是播放不了？
**A**: 检查以下几点：
1. CORS 设置：确保音频服务器允许跨域访问
2. HTTPS：如果网站是 HTTPS，音频也必须是 HTTPS
3. 文件格式：确认浏览器支持该格式
4. 文件大小：太大的文件可能加载慢

### Q: 如何设置 CORS？
**A**: 在 Supabase Storage:
1. Storage Settings
2. CORS Configuration
3. 添加允许的源

### Q: 推荐的音频参数？
**A**:
- 格式：MP3
- 比特率：128 kbps
- 采样率：44.1 kHz
- 声道：Mono（单声道，更小的文件）

## 立即行动

### 快速方案（2分钟）
```sql
-- 使用 BBC Learning English 音频
UPDATE tech_podcasts
SET audio_url = 'https://downloads.bbc.co.uk/learningenglish/features/6min/230831_6min_english_ai_and_music_download.mp3'
WHERE title = 'The Future of AI: Opportunities and Challenges';
```

### 长期方案（推荐）
1. 创建 Supabase Storage Bucket
2. 使用 TTS 生成自定义音频
3. 上传并获取公开URL
4. 更新数据库

---

**选择一个方案，让播客马上可以播放！** 🎧
